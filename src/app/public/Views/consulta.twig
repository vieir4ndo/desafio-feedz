{% extends 'template.twig' %}

{% block conteudo %}
<h3 class="text-left font-weight-bolder " >Consulta Usuários</h3>

<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nome</th>
      <th scope="col">E-mail</th>
    </tr>
  </thead>
  <tbody>
{% for usuario in usuarios %}
    <tr>
      <th scope="row">{{ usuario.id }}</th>
      <td>{{ usuario.nome }}</td>
      <td>{{ usuario.email }}</td>
    </tr>
{% endfor %}
</tbody>
</table>

{% endblock %}